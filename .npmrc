# NPM configuration for publishing packages
registry=https://registry.npmjs.org/

# Use legacy peer deps to handle peer dependency conflicts
# This is needed because @razorpay/blade has optional React Native peer deps
legacy-peer-deps=true

