name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Test Packages
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build packages in dependency order
        run: npm run build
        # Build tokens first (no dependencies), then dsm (depends on tokens)
        # This ensures type definitions are available for ESLint type checking
      
      - name: Check build artifacts
        run: |
          test -d packages/tokens/dist || exit 1
          test -d packages/dsm/dist || exit 1

      - name: Run linter
        run: npm run lint

      - name: Run type checking
        run: npm run typecheck

      - name: Run tests
        run: npm test -- --coverage

      - name: Upload coverage reports
        uses: codecov/codecov-action@v5
        if: matrix.node-version == '22.x'
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
          # Tokenless uploads are supported in v5 for public repos
          # For private repos, set CODECOV_TOKEN secret in GitHub repository settings
          # For public repos, ensure token authentication is disabled in Codecov org settings
