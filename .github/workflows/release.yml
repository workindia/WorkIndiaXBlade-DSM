name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  id-token: write # REQUIRED for trusted publishing

env:
  NPM_CONFIG_PROVENANCE: true # adds provenance to build

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          registry-url: 'https://registry.npmjs.org'
          # OIDC authentication - always-auth is handled automatically by setup-node@v4
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build packages and run tests before publishing
        run: npm run prepublish:ci

      - name: Verify npm authentication (debug)
        run: |
          echo "Checking npm authentication..."
          npm whoami || echo "⚠️ Not authenticated - this might be expected if OIDC is used"
          npm config get registry
          npm config list | grep -E "(registry|auth|token)" || echo "No auth config found (OIDC should handle this)"

      - name: Create Release Pull Request or Publish to npm
        id: changesets
        uses: changesets/action@v1
        with:
          publish: npm run release
          version: npm run version
          commit: 'chore: version packages'
          title: 'chore: version packages'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
